# ドキュメントルート変更
option_settings:
  aws:elasticbeanstalk:container:php:phpini:
    document_root: '/Polidog.Todo/public'

commands:
  # 日本時間にする
  1date:
    command: "sudo cp /usr/share/zoneinfo/Japan /etc/localtime"
